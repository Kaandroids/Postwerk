package com.postwerk.model.enums;

/**
 * Pricing model of a marketplace listing. Metadata only — no charge is performed on acquire.
 */
public enum PricingModel {
    FREE,
    ONE_TIME,
    MONTHLY,
    YEARLY,
    FREEMIUM
}
