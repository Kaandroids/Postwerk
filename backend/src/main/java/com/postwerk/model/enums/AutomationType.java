package com.postwerk.model.enums;

/**
 * Enumeration of supported automation workflow types.
 *
 * <p>Currently supports {@code EMAIL} for email-triggered automation workflows.
 * Additional types may be introduced as the platform evolves.</p>
 *
 * @since 1.0
 */
public enum AutomationType {
    EMAIL
}
