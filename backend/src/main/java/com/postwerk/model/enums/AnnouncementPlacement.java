package com.postwerk.model.enums;

/** Where an announcement is rendered to users. */
public enum AnnouncementPlacement {
    /** Top-of-app banner. */
    BANNER,
    /** In-app toast. */
    TOAST,
    /** Login-page card. */
    LOGIN
}
